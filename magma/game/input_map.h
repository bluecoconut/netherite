/* game/input_map.h - present-layer CrInput -> GmAction translation.
 *
 * INPUT module. Owns gm_input_reset / gm_input_map (declared in game/game.h).
 * Pure translation with a tiny internal static state (attack/use edge detection
 * and the wheel-cycled hotbar selection). Call gm_input_reset() once at startup.
 */
#ifndef MAGMA_GAME_INPUT_MAP_H
#define MAGMA_GAME_INPUT_MAP_H

#include "game/game.h"

#endif /* MAGMA_GAME_INPUT_MAP_H */
